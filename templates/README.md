Template folders should contain:

- `front.png`
- `back.png`
- `fields.json`

The current scaffold includes `fields.json` mappings and will fall back to a placeholder canvas if PNG assets are not present yet.
